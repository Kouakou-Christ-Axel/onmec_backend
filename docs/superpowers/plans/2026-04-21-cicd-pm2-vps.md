# CI/CD PM2 VPS Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Remplacer le déploiement Docker par GitHub Actions → SSH → PM2 avec PostgreSQL installé directement sur le VPS.

**Architecture:** Le job CI vérifie la compilation et les tests sur chaque push. Le job Deploy se déclenche uniquement sur master après un CI vert : il se connecte en SSH au VPS, pull le code, installe les dépendances, exécute les migrations Prisma avec `migrate deploy`, build l'app et recharge PM2 sans downtime.

**Tech Stack:** GitHub Actions, PM2, Node.js 22 (nvm), pnpm, PostgreSQL 15 (apt), Prisma 7, NestJS

---

### Task 1 : Fichier de config PM2

**Files:**
- Create: `ecosystem.config.js`
- Create: `logs/.gitkeep`

- [ ] **Step 1 : Créer le répertoire logs**

```bash
mkdir -p logs
touch logs/.gitkeep
```

- [ ] **Step 2 : Créer `ecosystem.config.js`**

```javascript
module.exports = {
  apps: [
    {
      name: 'onmec-api',
      script: 'dist/main.js',
      exec_mode: 'cluster',
      instances: 'max',
      env_production: {
        NODE_ENV: 'production',
        PORT: 8081,
      },
      error_file: './logs/error.log',
      out_file: './logs/out.log',
      log_date_format: 'YYYY-MM-DD HH:mm:ss',
      max_memory_restart: '500M',
      restart_delay: 3000,
    },
  ],
};
```

- [ ] **Step 3 : Vérifier la syntaxe du fichier**

```bash
node -e "const cfg = require('./ecosystem.config.js'); console.log('OK:', cfg.apps[0].name)"
```

Résultat attendu : `OK: onmec-api`

- [ ] **Step 4 : Ajouter `logs/` au `.gitignore`**

Ajouter à la fin de `.gitignore` :
```
logs/*.log
```

Garder `logs/.gitkeep` tracké (il crée le répertoire sur le VPS après `git pull`).

- [ ] **Step 5 : Commit**

```bash
git add ecosystem.config.js logs/.gitkeep .gitignore
git commit -m "ajout de la config PM2 pour le déploiement sans Docker"
```

---

### Task 2 : Script de setup VPS (one-time)

**Files:**
- Create: `scripts/setup-vps.sh`

- [ ] **Step 1 : Créer le répertoire scripts**

```bash
mkdir -p scripts
```

- [ ] **Step 2 : Créer `scripts/setup-vps.sh`**

```bash
#!/bin/bash
set -e

# ============================================================
# Setup one-time VPS pour onmec_backend
# À exécuter une seule fois en tant que root ou avec sudo
# Ubuntu 22.04 / 24.04
# ============================================================

APP_USER="onmec"
APP_DIR="/home/$APP_USER/onmec_backend"
DB_NAME="onmec"
DB_USER="onmec_user"
NODE_VERSION="22"

echo "=== [1/7] Création de l'utilisateur applicatif ==="
id -u "$APP_USER" &>/dev/null || useradd -m -s /bin/bash "$APP_USER"

echo "=== [2/7] Installation de Node.js $NODE_VERSION via nvm ==="
su - "$APP_USER" -c "
  curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.7/install.sh | bash
  export NVM_DIR=\"\$HOME/.nvm\"
  source \"\$NVM_DIR/nvm.sh\"
  nvm install $NODE_VERSION
  nvm alias default $NODE_VERSION
  corepack enable
  corepack prepare pnpm@latest --activate
"

echo "=== [3/7] Installation de PM2 ==="
su - "$APP_USER" -c "
  export NVM_DIR=\"\$HOME/.nvm\"
  source \"\$NVM_DIR/nvm.sh\"
  pnpm add -g pm2
  pm2 startup systemd -u $APP_USER --hp /home/$APP_USER | tail -1 | bash || true
"

echo "=== [4/7] Installation de PostgreSQL 15 ==="
apt-get update -q
apt-get install -y postgresql-15 postgresql-client-15

echo "=== [5/7] Création de la base de données et de l'utilisateur ==="
# Remplacer CHANGE_ME par un mot de passe sécurisé
DB_PASSWORD="CHANGE_ME"
su - postgres -c "
  psql -tc \"SELECT 1 FROM pg_roles WHERE rolname='$DB_USER'\" | grep -q 1 || \
    psql -c \"CREATE USER $DB_USER WITH PASSWORD '$DB_PASSWORD';\"
  psql -tc \"SELECT 1 FROM pg_database WHERE datname='$DB_NAME'\" | grep -q 1 || \
    psql -c \"CREATE DATABASE $DB_NAME OWNER $DB_USER;\"
"

echo "=== [6/7] Clone du repository ==="
su - "$APP_USER" -c "
  mkdir -p $APP_DIR
  git clone https://github.com/TON_ORG/onmec_backend.git $APP_DIR || \
    (cd $APP_DIR && git pull origin master)
"

echo "=== [7/7] Instructions manuelles restantes ==="
echo ""
echo "  1. Copier le fichier .env.production sur le VPS :"
echo "     scp .env.production $APP_USER@VPS_IP:$APP_DIR/.env.production"
echo ""
echo "  2. Premier déploiement manuel :"
echo "     su - $APP_USER"
echo "     cd $APP_DIR"
echo "     source ~/.nvm/nvm.sh"
echo "     pnpm install --frozen-lockfile"
echo "     npx prisma generate"
echo "     npx prisma migrate deploy"
echo "     pnpm build"
echo "     pm2 start ecosystem.config.js --env production"
echo "     pm2 save"
echo ""
echo "  3. Ajouter les secrets GitHub (Settings > Secrets > Actions) :"
echo "     SSH_KEY  = clé privée SSH de l'utilisateur $APP_USER"
echo "     SSH_USER = $APP_USER"
echo "     SSH_HOST = IP ou hostname du VPS"
echo ""
echo "=== Setup terminé ==="
```

- [ ] **Step 3 : Rendre le script exécutable**

```bash
chmod +x scripts/setup-vps.sh
```

- [ ] **Step 4 : Vérifier la syntaxe bash**

```bash
bash -n scripts/setup-vps.sh && echo "Syntaxe OK"
```

Résultat attendu : `Syntaxe OK`

- [ ] **Step 5 : Commit**

```bash
git add scripts/setup-vps.sh
git commit -m "ajout du script de setup one-time VPS"
```

---

### Task 3 : Réécriture du pipeline GitHub Actions

**Files:**
- Modify: `.github/workflows/deploy.yml`

- [ ] **Step 1 : Réécrire `.github/workflows/deploy.yml`**

Remplacer le contenu complet par :

```yaml
name: CI/CD

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

jobs:
  ci:
    name: CI
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: latest

      - name: Setup Node.js 22
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Generate Prisma client
        run: npx prisma generate

      - name: Type check
        run: npx tsc --noEmit

      - name: Run tests
        run: pnpm test

  deploy:
    name: Deploy
    needs: ci
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/master' && github.event_name == 'push'
    timeout-minutes: 30

    steps:
      - name: Setup SSH
        uses: webfactory/ssh-agent@v0.9.0
        with:
          ssh-private-key: ${{ secrets.SSH_KEY }}

      - name: Deploy to VPS
        timeout-minutes: 25
        run: |
          ssh -o StrictHostKeyChecking=no ${{ secrets.SSH_USER }}@${{ secrets.SSH_HOST }} << 'ENDSSH'
            set -e
            export NVM_DIR="$HOME/.nvm"
            source "$NVM_DIR/nvm.sh"
            cd ~/onmec_backend
            git pull origin master
            pnpm install --frozen-lockfile
            npx prisma generate
            npx prisma migrate deploy
            pnpm build
            pm2 reload ecosystem.config.js --env production
          ENDSSH
```

- [ ] **Step 2 : Vérifier la syntaxe YAML**

```bash
python3 -c "import yaml; yaml.safe_load(open('.github/workflows/deploy.yml'))" && echo "YAML valide"
```

Résultat attendu : `YAML valide`

- [ ] **Step 3 : Vérifier que `checkout@v4` et `setup-node@v4` existent**

Les versions utilisées sont :
- `actions/checkout@v4` ✅ (dernière stable)
- `pnpm/action-setup@v4` ✅ (dernière stable)
- `actions/setup-node@v4` ✅ (dernière stable)
- `webfactory/ssh-agent@v0.9.0` ✅ (dernière stable)

- [ ] **Step 4 : Commit**

```bash
git add .github/workflows/deploy.yml
git commit -m "réécriture du pipeline CI/CD GitHub Actions sans Docker (PM2 + VPS)"
```

---

### Task 4 : Vérification finale locale

**Files:** aucun fichier modifié — vérifications seulement

- [ ] **Step 1 : Vérifier que `pnpm build` fonctionne**

```bash
pnpm build
```

Résultat attendu : build réussi dans `dist/`, fichier `dist/main.js` présent.

```bash
ls dist/main.js
```

- [ ] **Step 2 : Vérifier que l'entrée PM2 pointe sur le bon fichier**

```bash
node -e "
  const cfg = require('./ecosystem.config.js');
  const script = cfg.apps[0].script;
  const fs = require('fs');
  if (!fs.existsSync(script)) throw new Error(script + ' introuvable');
  console.log('OK: ' + script + ' existe');
"
```

Résultat attendu : `OK: dist/main.js existe`

- [ ] **Step 3 : Vérifier que `npx tsc --noEmit` passe toujours**

```bash
npx tsc --noEmit
```

Résultat attendu : aucune erreur (sortie vide, code 0).

- [ ] **Step 4 : Commit final si des ajustements ont été faits**

```bash
git add -A
git status  # vérifier qu'il n'y a rien d'inattendu
git commit -m "vérifications post-implémentation CI/CD" || echo "Rien à committer"
```
